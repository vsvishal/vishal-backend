// We will handly Api errors using ApiResponse() only with status code less than 400
// otherwise will use ApiErrors

class ApiResponse {
  constructor(statusCode, data, message = "Success") {
    this.statusCode = statusCode;
    this.data = data;
    this.message = message;
    this.success = statusCode < 400;
  }
}
